Good, we have a good organized folder: vlm-pose/

Now I need to improve my pose VLM dataset quality by modifying the data extraction process. Here's what I want to implement:

**Current Issue:** My pose VLM doesn't achieve high performance because the dataset has low-quality poses extracted from a range before contact points.

**Goal:** Create a high-quality dataset with poses very close to contact points and ensure pose consistency within skills.

**Key Changes:**

1. **Contact Timestep Detection (`detect_contact_timesteps_v2.py`)**:
	 - please learn and imitate parts from vlm-pose/data_preparation/extract_local_pairs.py
   - Detect FIRST contact between any robot component and any object part
   - Handle multiple contacts per demo (use only the first one)
   - Save contact timesteps as `contact_timesteps.json` file
   - Format: `skill_name: {demo_key: contact_timestep_index, ...}`


2.1. **Single Skill Task Filtering**:
   - Use only 44 single skill tasks from LIBERO (instead of all 90)
   - Task list already saved as `vlm-pose/data_preparation/single_skill_tasks_44.json`
   - **Important**: Task names in JSON are base names, actual files may have extensions like `_demo.hdf5`
   - Need to handle file name matching (e.g., "KITCHEN_SCENE1_open_the_drawer" matches "KITCHEN_SCENE1_open_the_drawer_demo.hdf5")


2.2. **Pose Extraction Window (`extract_local_pairs_v2.py`)**:
   - above change 2.1 is contained in this script. For this script, please learn and imitate parts from vlm-pose/data_preparation/extract_local_pairs.py
   - Use window `[-4, -3, -2]` relative to contact timestep (0 = contact) - this shall be a param, but default set as [-4, -3, -2]
   - Extract exactly 3 poses per demonstration
   - `-2` = last pose before contact (most important)
   - `-3, -4` = poses slightly further back
   - Load contact timesteps from `contact_timesteps.json`

4. **Pose Similarity Validation (`validate_pose_similarity.py`)**:
   - **Statistics Mode** (flag=True): Calculate statistics only
     - Average pose distance within each skill
     - Average position distance within each skill
     - Average orientation distance within each skill
     - Standard deviation of distances
     - Generate detailed statistics report
   - **Filtering Mode** (flag=False): Filter out different poses
     - Compute pose distance matrix per skill
     - Filter out poses that are too different from skill average
     - Maintain pose consistency within skills
   - **Implementation**: Separate into 2 methods/classes for clean code

**Implementation Details:**

**File Structure:**
vlm-pose/data_preparation/
├── single_skill_tasks_44.json       # ✅ DONE: 44 task names
├── extract_local_pairs_v2.py        # NEW: Modified extraction
├── detect_contact_timesteps_v2.py   # NEW: First contact detection
├── validate_pose_similarity.py      # NEW: Pose similarity validation
└── contact_timesteps.json           # Generated: Contact timesteps


**Contact Detection Logic:**
- Use `EE_GEOM_NAMES` to identify robot components
- Detect first contact between any robot component and any object
- Ignore subsequent contacts in same demo
- Handle edge cases (no contact found, multiple contacts)

**Pose Extraction Logic:**
- Load contact timesteps from `contact_timesteps.json`
- Extract poses at `[contact_timestep - 4, contact_timestep - 3, contact_timestep - 2]`
- Validate pose indices are within demo bounds
- Save poses with proper indexing

**Task Name Matching:**
- Load task names from `single_skill_tasks_44.json`
- Match base task names to actual demo files (e.g., "task_name" → "task_name_demo.hdf5")
- Handle file name variations and extensions

**Pose Similarity Validation:**
- **StatisticsMode Class**: Calculate and report statistics only
  - Compute average distances within skills
  - Generate detailed statistics report
  - No filtering, just analysis
- **FilteringMode Class**: Filter poses based on similarity
  - Compute distance matrix per skill
  - Apply filtering thresholds
  - Remove poses that are too different

**Files to reference:**
- `vlm-pose/data_preparation/extract_local_pairs.py` (base file to modify)
- `vlm-pose/data_preparation/README.md` (understand current structure)
- `vlm-pose/data_preparation/single_skill_tasks_44.json` (task list)

Please implement these changes to create a high-quality dataset for pose VLM training. Focus on accurate contact detection, precise pose extraction near contact points, proper task name matching, and clean separation of statistics vs filtering modes.

Please also generate test files in vlm-pose/tests, they shall all be used to test your generated scripts and you shall pass these test created by yourself. after all these steps, ask me for permission to run those python script one after one generated by you.